# 🚀 Локальный запуск проекта

## Шаг 1: Установка зависимостей

```bash
# Убедитесь что Go установлен (версия 1.20+)
go version

# Установите зависимости
go get github.com/PuerkitoBio/goquery
```

## Шаг 2: Настройка конфигурации

```bash
# Скопируйте пример конфигурации
cp config.example.json config.json

# Отредактируйте config.json
nano config.json
```

В `config.json` укажите:
- `BOT_TOKEN` - токен вашего Telegram бота от @BotFather
- `USER_ID` - ваш Telegram ID (получите через @userinfobot)
- `NOTIFICATION_MINUTES` - за сколько минут присылать уведомления (рекомендуется 15)

## Шаг 3: Сборка проекта

```bash
# Собрать основной бот
go build -o main main.go parser.go

# Собрать парсер (для тестов)
go build -o test_parser test_parser.go parser.go
```

Или используйте Makefile:

```bash
make build
```

## Шаг 4: Запуск

### Получить расписание

```bash
./test_parser
```

Это создаст файл `schedule.json` с вашим расписанием.

### Запустить бота

```bash
./main
```

Бот будет:
- Проверять расписание каждую минуту
- Отправлять уведомления за 15 минут до пар (кроме 3 пары - за 45 минут)
- Автоматически обновлять расписание каждый день в 2:00 ночи (MSK)

## 🔧 Полезные команды

```bash
# Пересобрать все
make clean && make build

# Запустить бота в фоне
nohup ./main > logs/bot.log 2>&1 &

# Посмотреть логи
tail -f logs/bot.log

# Остановить бота
pkill -f "./main"
```

## 🐛 Решение проблем

### Ошибка "command not found: go"
Установите Go с https://go.dev/dl/

### Ошибка "cannot find package goquery"
```bash
go get -u github.com/PuerkitoBio/goquery
go mod tidy
```

### Не приходят уведомления
1. Проверьте что BOT_TOKEN и USER_ID правильные
2. Убедитесь что вы написали боту `/start` в Telegram
3. Проверьте что `schedule.json` существует и содержит пары

### schedule.json пустой
Проверьте что вы можете зайти на https://tt.audit.msu.ru/
