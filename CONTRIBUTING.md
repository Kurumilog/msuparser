# 🤝 Внесение вклада

Спасибо что интересуешься проектом! Вот как можешь помочь.

## Сообщение об ошибках

Если нашёл баг:

1. Проверь что это не известная проблема в Issues
2. Предоставь:
   - Описание проблемы
   - Шаги для воспроизведения
   - Вывод ошибки (логи)
   - Версия ПО (Python, Go, ОС)

## Предложение улучшений

1. Опиши идею в issue
2. Обсудим подход перед началом работы

## Создание Pull Request

1. **Форкни репо**
2. **Создай ветку** для своей фишки (`git checkout -b feature/amazing-feature`)
3. **Коммитьте изменения** (`git commit -m 'Add amazing feature'`)
4. **Пушьте в ветку** (`git push origin feature/amazing-feature`)
5. **Откройте Pull Request**

## Стайл кода

### Python
- PEP 8
- 4 пробела для отступов
- Максимум 120 символов в строке

```python
def hello(name: str) -> str:
    """Функция приветствия."""
    return f"Hello, {name}!"
```

### Go
- `gofmt` для форматирования
- Комментарии для экспортируемых функций

```go
// Hello returns a greeting message.
func Hello(name string) string {
    return fmt.Sprintf("Hello, %s!", name)
}
```

## Тестирование перед push

```bash
# Python
python parser_full.py

# Go
go build -o bot main.go
./bot
```

## Вопросы?

Создавай issue или обсуди в discussions!

---

Спасибо за вклад! 🚀
