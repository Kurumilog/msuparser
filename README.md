# 🎓 MSU Timetable Bot

Автоматизированная система для получения уведомлений о расписании занятий МГУ ВШГА.

## 📋 Описание

**Современный Go-сервис** для парсинга расписания МГУ и отправки уведомлений в Telegram.

### Основные возможности

✅ **Нативный парсер на Go** - без Selenium, только HTTP запросы  
✅ **Умные уведомления** - учитывает дистанционные и обычные пары  
✅ **Автоматическое обновление** - ежедневно в 2:00 ночи (MSK)  
✅ **Низкое потребление ресурсов** - всего ~20-50 MB RAM  
✅ **Легкое развертывание** - один бинарник, без зависимостей  

### Особенности уведомлений

- **Обычные пары**: Уведомление за 15 минут до начала
- **3 пара**: Уведомление за 45 минут (в 12:15 если пара в 13:00)
- **Дистанционные пары** (если ВСЕ пары дистанционные): Одно утреннее уведомление в 8:00 со списком
- **Смешанный день** (есть очные): Индивидуальные уведомления для всех пар

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Клонируем репозиторий
git clone https://github.com/Kurumilog/msuparser.git
cd msuparser

# Устанавливаем зависимости
go get github.com/PuerkitoBio/goquery

# Настраиваем конфигурацию
cp config.example.json config.json
nano config.json  # Заполните BOT_TOKEN и USER_ID

# Собираем
go build -o main main.go parser.go
go build -o test_parser test_parser.go parser.go

# Запускаем парсер
./test_parser

# Запускаем бота
./main
```

### Развертывание на сервере

См. **[DEPLOY_GUIDE.md](DEPLOY_GUIDE.md)** - полное руководство по установке на Ubuntu.

## 📚 Документация

- **[DEPLOY_GUIDE.md](DEPLOY_GUIDE.md)** - Установка на Ubuntu сервер
- **[QUICKSTART_GO.md](QUICKSTART_GO.md)** - Быстрый старт с Go парсером
- **[PARSER_README.md](PARSER_README.md)** - Документация парсера
- **[TECHNICAL_DETAILS.md](TECHNICAL_DETAILS.md)** - Технические детали

## 🔧 Конфигурация

Создайте `config.json`:

```json
{
  "BOT_TOKEN": "your-telegram-bot-token",
  "USER_ID": "your-telegram-user-id",
  "NOTIFICATION_MINUTES": 15
}
```

**Как получить:**
- **BOT_TOKEN**: [@BotFather](https://t.me/BotFather) → `/newbot`
- **USER_ID**: [@userinfobot](https://t.me/userinfobot)

## 📁 Структура проекта

```
msuparser/
├── main.go                      # Telegram бот
├── parser.go                    # Парсер расписания (Go)
├── test_parser.go               # Тестовый запуск парсера
├── config.json                  # Конфигурация
├── schedule.json                # Кэш расписания
├── msuparser-bot.service        # Systemd сервис бота
├── msuparser-update.service     # Systemd сервис обновления
├── msuparser-update.timer       # Таймер обновления (раз в 3 дня)
└── DEPLOY_GUIDE.md             # Гайд по развертыванию
```

## 🎯 Как это работает

1. **Парсер** (`test_parser`) собирает расписание с tt.audit.msu.ru
2. Сохраняет в `schedule.json`
3. **Бот** (`main`) читает расписание и отправляет уведомления
4. **Systemd timer** запускает парсер раз в 3 дня

### Дистанционные пары

Бот определяет дистанционные пары автоматически по ключевым словам:
- `дистанц`
- `виртуал`

Вместо уведомления перед каждой парой, бот отправляет **одно сообщение утром**:

```
📱 Напоминание

У вас сегодня дистанционные пары:

• 1 пара (09:00-10:30)
  📚 Международное право [Сем]
  👨‍🏫 Пименова Софья Дмитриевна
```

## 🛠️ Управление

### Локально

```bash
# Обновить расписание
./test_parser

# Запустить бота
./main
```

### На сервере (systemd)

```bash
# Статус
sudo systemctl status msuparser-bot

# Логи
sudo journalctl -u msuparser-bot -f

# Перезапуск
sudo systemctl restart msuparser-bot

# Ручное обновление расписания
sudo systemctl start msuparser-update
```

## 📊 Производительность

| Метрика | Значение |
|---------|----------|
| Время парсинга | 1-3 секунды |
| Использование памяти | ~20-50 MB |
| Размер бинарника | 9.5 MB |
| CPU usage | <1% |

**Сравнение с Python:**
- ⚡ В 3-5 раз быстрее
- 💾 В 5-10 раз меньше памяти
- 📦 В 5 раз меньше размер
- 🚀 Не требует внешних драйверов

## 🔄 Обновление

```bash
cd ~/msuparser
git pull
go build -o main main.go parser.go
go build -o test_parser test_parser.go parser.go
sudo systemctl restart msuparser-bot
```

## 🐛 Troubleshooting

### Бот не отправляет уведомления

1. Проверьте `config.json` - правильность BOT_TOKEN и USER_ID
2. Напишите боту `/start`
3. Проверьте часовой пояс: `timedatectl` (должен быть Europe/Minsk)

### Парсер не работает

```bash
# Запустите вручную
./test_parser

# Проверьте вывод
cat schedule.json | head -20
```

### Логи показывают ошибки

```bash
# Смотрим последние ошибки
sudo journalctl -u msuparser-bot -p err -n 50
```

## 📝 Разработка

### Требования

- Go 1.24+
- github.com/PuerkitoBio/goquery

### Сборка

```bash
# Парсер
go build -o test_parser test_parser.go parser.go

# Бот
go build -o main main.go parser.go

# Makefile
make build        # Собрать парсер
make build-main   # Собрать бота
make clean        # Очистить
```

### Тестирование

```bash
# Тест парсера
./test_parser

# Тест бота (с существующим schedule.json)
./main
```

## 📄 Лицензия

MIT License - см. [LICENSE](LICENSE)

## 🤝 Вклад

Приветствуются Pull Requests! См. [CONTRIBUTING.md](CONTRIBUTING.md)

## 📞 Контакты

- GitHub: [@Kurumilog](https://github.com/Kurumilog)
- Issues: [github.com/Kurumilog/msuparser/issues](https://github.com/Kurumilog/msuparser/issues)

---

**Сделано с ❤️ для студентов МГУ ВШГА**

## 🚀 Быстрый старт

### Требования

- Python 3.8+
- Go 1.21+
- Chrome или Firefox (для парсера)
- Telegram бот и токен

### Установка

1. **Клонируем репозиторий:**
```bash
git clone <repo-url>
cd msuparser
```

2. **Создаём виртуальное окружение Python:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows
```

3. **Устанавливаем зависимости Python:**
```bash
pip install -r requirements.txt
```

4. **Обновляем конфиг с вашими данными:**

Отредактируй файл `config.py`:
```python
BOT_TOKEN = "ваш_токен_от_@BotFather"
USER_ID = "ваш_telegram_id_от_@userinfobot"
```

5. **Компилируем Go бот:**
```bash
go build -o bot main.go
chmod +x bot
```

6. **Даём права на выполнение скрипту:**
```bash
chmod +x update_schedule.sh
```

## 📦 Использование

### Вариант 1: Ручной запуск (для тестирования)

**Запуск парсера:**
```bash
source venv/bin/activate
python parser_full.py
```

**Запуск бота:**
```bash
./bot
```

### Вариант 2: С systemd (автоматический запуск)

Подробная инструкция по настройке сервера Ubuntu: [SERVER_SETUP.md](SERVER_SETUP.md)

**Кратко:**
```bash
./install_services.sh
```

Это установит:
- `msuparser-bot.service` - бот, запускается при загрузке
- `msuparser-update.timer` - таймер для парсера
- `msuparser-update.service` - сервис парсера

**Полезные команды:**
```bash
# Статус бота
systemctl --user status msuparser-bot

# Логи бота (в реальном времени)
journalctl --user -u msuparser-bot -f

# Статус таймера обновления
systemctl --user status msuparser-update.timer

# Логи парсера
journalctl --user -u msuparser-update -f

# Остановить бота
systemctl --user stop msuparser-bot

# Перезапустить бота
systemctl --user restart msuparser-bot

# Отключить автозагрузку
systemctl --user disable msuparser-bot.service
systemctl --user disable msuparser-update.timer
```

## 📅 Как это работает

1. **Первый запуск парсера** - собирает расписание на 5 дней и сохраняет в `schedule.json`
2. **Бот запускается** и начинает отслеживать предстоящие пары
3. **Каждую минуту** бот проверяет расписание
4. **За 15 минут** до начала пары отправляется Telegram уведомление
5. **Раз в 3 дня** парсер автоматически обновляет расписание

## 🔧 Структура проекта

```
msuparser/
├── parser_full.py           # Парсер расписания (Python + Selenium)
├── bot.py                   # Старый бот (Python + async)
├── main.go                  # Новый бот (Go + sync)
├── config.py                # Конфигурация (нужно заполнить)
├── requirements.txt         # Зависимости Python
├── go.mod                   # Модули Go
├── update_schedule.sh       # Скрипт обновления парсера
├── install_services.sh      # Установка systemd сервисов
├── msuparser-bot.service    # Systemd сервис бота
├── msuparser-update.service # Systemd сервис парсера
├── msuparser-update.timer   # Systemd таймер
├── schedule.json            # Расписание (генерируется)
├── .gitignore               # Исключения для git
└── README.md                # Этот файл
```

## 📊 Формат schedule.json

```json
[
  {
    "subject": "Международное право[Сем]",
    "teacher": "Алиев Шамиль Муртузович",
    "room": "ауд. 3027А",
    "lesson_number": "1",
    "time_start": "09:00",
    "time_end": "10:30",
    "date": "01.12.2025",
    "weekday": "Пн",
    "group": "303"
  },
  ...
]
```

## 🔔 Пример уведомления

```
🔔 Скоро пара!

📚 Предмет: Международное право[Сем]
👨‍🏫 Преподаватель: Алиев Шамиль Муртузович
🚪 Аудитория: ауд. 3027А

🕐 Время: 09:00 - 10:30
📅 Дата: 01.12.2025 (Пн)
```

## 🐛 Решение проблем

### ChromeDriver версия не совпадает

Если при запуске парсера ошибка про ChromeDriver:
```bash
# Очистить кэш webdriver-manager
rm -rf ~/.wdm/

# Пересоздать виртуальное окружение
rm -rf venv
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Бот не отправляет уведомления

1. Проверь токен и ID в `config.py`
2. Проверь наличие файла `schedule.json`
3. Посмотри логи:
```bash
journalctl --user -u msuparser-bot -f
```

### Парсер не запускается в systemd

1. Проверь пути в `msuparser-update.service`
2. Посмотри логи:
```bash
journalctl --user -u msuparser-update -f
```

## 🤝 Внесение изменений

1. Отредактируй файлы
2. Протестируй локально:
```bash
source venv/bin/activate
python parser_full.py
./bot &
```
3. Проверь логи systemd (если используешь)
4. Сделай commit и push

## 📝 Лицензия

MIT

## 📧 Контакты

Для вопросов и проблем создавай issue в репозитории.

---

**Последнее обновление:** 2025-11-29
