# 🚀 Быстрый старт за 5 минут

## Пошаговая инструкция

### 1. Подготовка (1 мин)

```bash
# Клонируй репозиторий
git clone <url>
cd msuparser

# Запусти автоматическую настройку
chmod +x setup.sh
./setup.sh
```

### 2. Конфигурация (2 мин)

Найди в Telegram:
- **@BotFather** - для получения BOT_TOKEN
- **@userinfobot** - для получения твоего USER_ID

Отредактируй `config.py`:
```python
BOT_TOKEN = "123456789:ABCdefGHIjklmnoPQRstUVwxyzABCDefGHI"
USER_ID = "1234567890"
```

### 3. Первый запуск парсера (1 мин)

```bash
source venv/bin/activate
python parser_full.py
```

Парсер загрузит расписание на 5 дней в `schedule.json`.

### 4. Запуск бота (1 мин)

```bash
./bot
```

Бот будет проверять расписание каждую минуту и отправлять уведомления за 15 минут до пары.

## Автоматический запуск при загрузке

### Вариант 1: Простой скрипт (без systemd)

```bash
# В фоне
./start.sh

# Для остановки
./stop.sh
```

### Вариант 2: Через systemd (рекомендуется)

```bash
./install_services.sh
```

Отслеживать работу:
```bash
# Статус
systemctl --user status msuparser-bot

# Логи в реальном времени
journalctl --user -u msuparser-bot -f
```

## Проверка работы

```bash
# Протестировать бот
./test_bot.sh

# Посмотреть расписание
cat schedule.json | python -m json.tool | head -20

# Проверить логи
tail -f logs/bot.log
```

## Часто задаваемые вопросы

**Q: Бот не отправляет уведомления**
A: Проверь:
1. Токен и ID в `config.py`
2. Наличие `schedule.json`
3. Логи: `journalctl --user -u msuparser-bot -f`

**Q: Расписание не загружается**
A: Проверь:
1. Интернет соединение
2. Есть ли браузер (Chrome/Firefox)
3. Логи парсера: `tail -f logs/parser_bg.log`

**Q: Как обновить расписание вручную?**
A: `python parser_full.py`

**Q: Могу ли я менять интервал уведомлений?**
A: Да! В `config.py` измени `NOTIFICATION_MINUTES = 15` (или другое значение)

---

Для подробной информации см. [README.md](README.md)
