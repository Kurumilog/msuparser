# 📤 Как запушить проект на GitHub

## Вариант 1: Первая публикация (репозиторий уже создан)

Ваш репозиторий уже существует: https://github.com/Kurumilog/msuparser

```bash
# 1. Добавьте все файлы (кроме тех что в .gitignore)
git add .

# 2. Создайте коммит с описанием изменений
git commit -m "feat: migrate to Go parser with smart notifications

- Native Go parser without Selenium
- Smart notifications for distance learning
- Daily schedule updates at 2:00 AM MSK
- Special timing for 3rd class (45 min before)
- Moscow timezone support"

# 3. Отправьте на GitHub
git push origin master
```

## Вариант 2: Если нужно переименовать ветку в main

```bash
# Переименовать локальную ветку
git branch -m master main

# Отправить и установить upstream
git push -u origin main

# Удалить старую ветку master на GitHub (если нужно)
git push origin --delete master
```

## Вариант 3: Если возникла ошибка "rejected"

Это значит на GitHub есть изменения, которых нет у вас локально.

```bash
# Получить изменения с GitHub
git pull origin master --rebase

# Если есть конфликты - разрешите их
# Затем продолжите:
git rebase --continue

# Отправьте снова
git push origin master
```

## 🔐 Важно: Безопасность

### Перед пушем проверьте:

1. **config.json НЕ должен попасть в Git** (он уже в .gitignore)
2. **Проверьте что токены не в коде:**

```bash
# Проверить что config.json игнорируется
git status

# Если config.json виден - добавьте в .gitignore:
echo "config.json" >> .gitignore
```

3. **Если случайно запушили токены:**

```bash
# Удалите файл из истории
git rm --cached config.json
git commit -m "Remove sensitive config"
git push origin master

# ЗАТЕМ СРАЗУ:
# - Создайте новый токен бота через @BotFather
# - Обновите config.json локально
```

## 📝 Полезные команды Git

```bash
# Посмотреть статус (какие файлы изменены)
git status

# Посмотреть разницу в файлах
git diff

# История коммитов
git log --oneline

# Отменить последний коммит (если еще не запушили)
git reset --soft HEAD~1

# Посмотреть что игнорируется
git status --ignored
```

## 🏷️ Создание релиза (опционально)

После успешного пуша можете создать релиз:

```bash
# Создать тег
git tag -a v2.0.0 -m "Version 2.0.0 - Go rewrite"

# Отправить теги на GitHub
git push origin --tags
```

Затем на GitHub:
1. Перейдите в Releases
2. Нажмите "Create a new release"
3. Выберите тег v2.0.0
4. Добавьте описание изменений
5. Можете приложить бинарники (main, test_parser)

## 🤝 Workflow для дальнейшей работы

```bash
# 1. Вносите изменения в файлы
nano main.go

# 2. Проверяйте что работает
go build -o main main.go parser.go
./main

# 3. Коммитьте и пушьте
git add main.go
git commit -m "fix: improve notification timing"
git push origin master
```
